# X API (旧Twitter、write accessには4つのキーが必要)
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=
# Optional: read-only bearer if needed
X_BEARER_TOKEN=

# Google Gemini API
GEMINI_API_KEY=

# Meta Threads API (requires business account and app approval)
# Option 1: Direct access token (recommended for production)
THREADS_ACCESS_TOKEN=
THREADS_USER_ID=

# Option 2: App credentials (will generate access token automatically)
THREADS_APP_ID=
THREADS_APP_SECRET=
# THREADS_USER_ID= (still required for both options)

# Optional: runtime controls
# CRON_SCHEDULE=0 9 * * *
# NODE_ENV=development
